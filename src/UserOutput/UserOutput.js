import React from 'react';

const userOutput = (props) => {
    return (
        <div>
            <p>This is {props.name} relearning React</p>
            <p>userName: {props.userName} </p>
         </div>
    )
};


export default userOutput;
